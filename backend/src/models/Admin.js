/**
 * @typedef {Object} Admin
 * @property {number} id_admin
 * @property {string} usuario
 * @property {string} password_hash  Hash bcrypt, nunca texto plano
 * @property {string} nombre_completo
 * @property {string} rol
 */
module.exports = {};
